rules_version = '2'; 
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }
    
    function isOwner(uid) {
      return isSignedIn() && request.auth.uid == uid;
    }
    
    function isAdmin() {
      return isSignedIn() && (
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true) ||
        request.auth.token.email.lower() in ['admin@eliteunlock.com']
      );
    }

    function isNonEmptyStringList(arr) {
      return arr is list && arr.size() > 0 && arr[0] is string;
    }

    function isMailMessage(msg) {
      return msg is map
        && msg.keys().hasOnly(['subject','text','html'])
        && msg.subject is string
        && msg.text is string
        && msg.html is string;
    }
    
    // ===== users =====
    match /users/{uid} {
      allow read: if isOwner(uid) || isAdmin();
      
      allow create: if isOwner(uid)
        && request.resource.data.keys().hasOnly(['email','displayName','credits','createdAt','isAdmin'])
        && request.resource.data.email is string
        && request.resource.data.email.lower() == request.auth.token.email.lower()
        && request.resource.data.credits is int
        && request.resource.data.credits == 0
        && (!('isAdmin' in request.resource.data) || request.resource.data.isAdmin == false);
      
      // อนุญาตให้ user อัพเดต credits ของตัวเองได้ (สำหรับการซื้อสินค้า)
      allow update: if isAdmin()
        || (
          isOwner(uid)
          && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['credits'])
          && request.resource.data.credits is int
          && request.resource.data.credits <= resource.data.credits  // ลดได้อย่างเดียว ห้ามเพิ่ม
        )
        || (
          isOwner(uid)
          && (!request.resource.data.diff(resource.data).affectedKeys().hasAny(['credits','isAdmin','isAgent','role']))
        );
      
      allow delete: if false;
    }
    
    // ===== products =====
    match /products/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // ===== orders ===== 
    // แก้ไข: ให้ทุกคนที่ sign in แล้วสามารถสั่งซื้อได้
    match /orders/{id} {
      // อนุญาตให้สร้าง order ได้ถ้า sign in แล้ว
      allow create: if isSignedIn()
        && request.resource.data.userId == request.auth.uid;
      
      // อ่านได้ถ้าเป็นเจ้าของ order หรือเป็น admin
      allow read: if isSignedIn() && (
        resource.data.userId == request.auth.uid || isAdmin()
      );
      
      // แก้ไขได้เฉพาะ admin
      allow update: if isAdmin();
      allow delete: if false;
    }
    
    // ===== userSecurity =====
    match /userSecurity/{docId} {
      allow get: if true;
      
      allow create, update: if isSignedIn()
        && request.resource.data.keys().hasOnly(['emailLower','ownerUid','questionKey','answerHash','updatedAt'])
        && request.resource.data.ownerUid == request.auth.uid;
      
      allow delete: if false;
    }
    
    // ===== config =====
    match /config/{doc} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ===== banner =====
    match /banner/{doc} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ===== credit requests =====
    match /creditRequests/{id} {
      allow create: if isSignedIn()
        && request.resource.data.userId == request.auth.uid
        && request.resource.data.userEmail == request.auth.token.email
        && request.resource.data.amount > 0
        && request.resource.data.status == 'pending'
        && request.resource.data.proofImageUrl is string
        && request.resource.data.proofImageUrl.matches('https://.*');

      allow read: if isSignedIn() && (
        resource.data.userId == request.auth.uid || isAdmin()
      );

      allow list: if isSignedIn();
      allow update: if isAdmin();
      allow delete: if false;
    }

    // ===== mail queue =====
    match /mail/{docId} {
      allow read: if isAdmin();

      allow create: if isSignedIn()
        && request.resource.data.keys().hasAll(['to','from','message','createdAt'])
        && isNonEmptyStringList(request.resource.data.to)
        && request.resource.data.from is string
        && isMailMessage(request.resource.data.message)
        && request.resource.data.createdAt == request.time;

      allow update, delete: if false;
    }
  }
}