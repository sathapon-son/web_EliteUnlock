📋 วิธีการใช้ Python Scripts สำหรับ Migration Orders

================================================================================
ขั้นตอนที่ 1: ติดตั้ง Firebase Admin SDK
================================================================================

เปิด PowerShell และรันคำสั่ง:

```powershell
pip install firebase-admin
```

================================================================================
ขั้นตอนที่ 2: ดาวน์โหลด Service Account Key
================================================================================

1. ไปที่ Firebase Console: https://console.firebase.google.com
2. เลือก Project: "eliteunlock-c194e"
3. ไปที่ Settings (⚙️) → Service Accounts
4. คลิก "Generate New Private Key"
5. บันทึกไฟล์เป็น: serviceAccountKey.json
6. วางไฟล์นี้ในโฟลเดอร์เดียวกับ script (.py)

⚠️ ห้ามอัพโหลด serviceAccountKey.json ขึ้น Git/GitHub

================================================================================
ขั้นตอนที่ 3: ดึงข้อมูล Orders
================================================================================

รันไฟล์นี้เพื่อดูข้อมูล orders ทั้งหมด:

```powershell
python migrate_orders.py
```

ผลลัพธ์:
- แสดง summary ของ orders ทั้งหมด
- บันทึก orders ลงไฟล์ JSON สำรอง (orders_backup_YYYYMMDD_HHMMSS.json)

================================================================================
ขั้นตอนที่ 4: ทำ Migration (เปลี่ยน Document ID)
================================================================================

⚠️ ขั้นตอนนี้จะแก้ไขข้อมูลจริง!

รันไฟล์ migration:

```powershell
python do_migrate_orders.py
```

Script นี้จะ:
1. ดึงข้อมูล orders ทั้งหมด
2. สร้าง document ใหม่ด้วย Document ID = ลำดับเลข (1, 2, 3...)
3. ลบ document เก่า (Document ID ยาว ๆ)
4. บันทึกผล migration ลงไฟล์ JSON

================================================================================
ขั้นตอนที่ 5: ตรวจสอบผลลัพธ์
================================================================================

หลังจาก migration เสร็จ ให้ตรวจสอบ:
- ไปที่ Firebase Console → Firestore
- ตรวจสอบ collection "orders"
- ต้องเห็น Document ID: 1, 2, 3, 4... แทนที่ ID ยาว ๆ

================================================================================
📝 หมายเหตุ
================================================================================

- ดึงข้อมูลสำรองก่อน (migrate_orders.py)
- ทดสอบ script ด้วยข้อมูล test ก่อนทำจริง
- เก็บ serviceAccountKey.json ไว้ปลอดภัย

ถ้าต้องการความช่วยเหลือเพิ่มเติม ติดต่อ...
